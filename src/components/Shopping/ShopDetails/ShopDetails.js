import React from 'react'

const ShopDetails = (props) => {
    return (
        <div>
            <h2>Men's Shop</h2>

            {props.children}
        </div>
    )
}

export default ShopDetails
